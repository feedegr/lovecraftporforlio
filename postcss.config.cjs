module.exports = {
  plugins: {
    // Use the official Tailwind PostCSS plugin entry. If you upgrade to a Tailwind
    // release that requires a different plugin name, adjust accordingly.
    tailwindcss: {},
    autoprefixer: {},
  },
}
